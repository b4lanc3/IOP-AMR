<!--
  AUTO-GENERATED COPY. Do not edit this file directly.
  Source of truth: /docs/PROTOCOL.md
  Run: pwsh scripts/sync_protocol.ps1
-->

# Protocol â€” App â†” Robot (nguá»“n chuáº©n)

> **Nguá»“n tin cáº­y duy nháº¥t** vá» cÃ¡c topic, service, action, message mÃ  app Flutter vÃ  Jetson pháº£i thá»‘ng nháº¥t.
> `jetson/docs/PROTOCOL.md` lÃ  báº£n sao â€” Ä‘á»“ng bá»™ báº±ng `scripts/sync_protocol.ps1`.

## 1. WebSocket & HTTP endpoints

| Endpoint | Port | Giao thá»©c | Má»¥c Ä‘Ã­ch |
|---|---|---|---|
| rosbridge WebSocket | 9090 | ws / wss | JSON roslib protocol |
| web_video_server    | 8080 | HTTP   | MJPEG camera stream |
| rosapi (cÃ¹ng bridge)| 9090 | ws     | List topic/service Ä‘á»™ng |

URL WebSocket cho app: `ws://<jetson-ip>:9090`
URL camera RGB:       `http://<jetson-ip>:8080/stream?topic=/camera/color/image_raw&type=mjpeg`
URL camera Depth:     `http://<jetson-ip>:8080/stream?topic=/camera/depth/image_raw&type=mjpeg`

## 2. Topic app SUBSCRIBE (robot publish)

| Topic | Msg type | Rate | MÃ´ táº£ |
|---|---|---|---|
| `/scan` | `sensor_msgs/msg/LaserScan` | ~10 Hz | Hinson LiDAR 2D |
| `/map` | `nav_msgs/msg/OccupancyGrid` | on-change | Map tá»« slam_toolbox/map_server |
| `/odom` | `nav_msgs/msg/Odometry` | 30â€“50 Hz | Tá»« diff_drive_controller |
| `/amcl_pose` | `geometry_msgs/msg/PoseWithCovarianceStamped` | on-change | Khi dÃ¹ng AMCL |
| `/tf` | `tf2_msgs/msg/TFMessage` | 30â€“100 Hz | TF Ä‘á»™ng (odom â†’ base_link, map â†’ odom) |
| `/tf_static` | `tf2_msgs/msg/TFMessage` | 1Ã— | TF tÄ©nh URDF |
| `/joint_states` | `sensor_msgs/msg/JointState` | 30 Hz | Velocity 2 bÃ¡nh Kinco |
| `/plan` | `nav_msgs/msg/Path` | on-change | Global plan Nav2 |
| `/local_plan` | `nav_msgs/msg/Path` | 10 Hz | Local plan DWB |
| `/amr/system_stats` | `amr_integration/msg/SystemStats` | 1 Hz | CPU/GPU/RAM/temp |
| `/amr/battery` | `amr_integration/msg/Battery` | 1 Hz | Voltage/percent/current |

## 3. Topic app PUBLISH (robot subscribe)

| Topic | Msg type | Rate khi active |
|---|---|---|
| `/cmd_vel` | `geometry_msgs/msg/Twist` | 15 Hz trong teleop |
| `/initialpose` | `geometry_msgs/msg/PoseWithCovarianceStamped` | on-demand |

**Twist format chuáº©n**:
```json
{
  "linear":  { "x": 0.3, "y": 0.0, "z": 0.0 },
  "angular": { "x": 0.0, "y": 0.0, "z": 0.5 }
}
```

## 4. Service

| Service | Type | Má»¥c Ä‘Ã­ch |
|---|---|---|
| `/slam_toolbox/save_map` | `slam_toolbox/srv/SaveMap` | LÆ°u map sau mapping |
| `/amr/slam/control` | `amr_integration/srv/SlamControl` | start / stop / save / reset |
| `/amr/estop` | `amr_integration/srv/EStop` | Engage / release e-stop |
| `/amr/bag/control` | `amr_integration/srv/BagControl` | start / stop / list rosbag |
| `/<node>/get_parameters` | `rcl_interfaces/srv/GetParameters` | Äá»c param |
| `/<node>/set_parameters` | `rcl_interfaces/srv/SetParameters` | Tune param live |

## 5. Action

| Action | Type | Má»¥c Ä‘Ã­ch |
|---|---|---|
| `/navigate_to_pose` | `nav2_msgs/action/NavigateToPose` | Click-to-goal |
| `/navigate_through_poses` | `nav2_msgs/action/NavigateThroughPoses` | Waypoint mission |

## 6. Custom messages

### `amr_integration/msg/SystemStats`

```
float32 cpu_percent
float32 gpu_percent
float32 ram_used_mb
float32 ram_total_mb
float32 cpu_temp_c
float32 gpu_temp_c
float32 disk_used_gb
float32 disk_total_gb
builtin_interfaces/Time stamp
```

### `amr_integration/msg/Battery`

```
float32 voltage
float32 current
float32 percent
float32 temperature
bool    charging
builtin_interfaces/Time stamp
```

### `amr_integration/srv/EStop`

```
bool engage        # true = stop, false = release
---
bool success
string message
```

### `amr_integration/srv/BagControl`

```
string action      # "start" | "stop" | "list"
string bag_name
string[] topics
---
bool success
string message
string[] bags
```

### `amr_integration/srv/SlamControl`

```
string action      # "start" | "stop" | "save" | "reset"
string map_name
---
bool success
string message
```

## 7. Namespace multi-robot

Náº¿u váº­n hÃ nh nhiá»u robot trÃªn cÃ¹ng máº¡ng:

- Má»—i Jetson cháº¡y ROS vá»›i `ROS_DOMAIN_ID` khÃ¡c nhau, hoáº·c
- CÃ¹ng DOMAIN_ID nhÆ°ng remap namespace: `ros2 launch ... namespace:=robot_1`.

App lÆ°u `namespace` cho tá»«ng `RobotProfile` vÃ  prefix táº¥t cáº£ topic/service tÆ°Æ¡ng á»©ng.

## 8. XÃ¡c thá»±c (optional)

rosbridge máº·c Ä‘á»‹nh khÃ´ng auth. Vá»›i LAN tin cáº­y cÃ³ thá»ƒ bá» qua. Náº¿u cáº§n:

- Báº­t `authenticate: true` trong rosbridge config.
- App gá»­i service call `/rosauth/authenticate` Ä‘áº§u tiÃªn vá»›i `mac` HMAC theo doc [rosauth](https://wiki.ros.org/rosauth).

## 9. Kiá»ƒm tra nhanh tá»« Windows

```powershell
# Install wscat
npm install -g wscat
wscat -c ws://<jetson-ip>:9090

# Subscribe /scan
> {"op":"subscribe","topic":"/scan","type":"sensor_msgs/msg/LaserScan"}

# Publish /cmd_vel (tiáº¿n 0.2 m/s)
> {"op":"publish","topic":"/cmd_vel","msg":{"linear":{"x":0.2,"y":0,"z":0},"angular":{"x":0,"y":0,"z":0}}}
```
